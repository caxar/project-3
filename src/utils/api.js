// import axios from "axios";

// const params = {
//   headers: {
//     Authorization: "Basic " + process.env.OPEN_WEATHER_API_KEY,
//   },
// };

// export const fetchDataFromApi = async (url) => {
//   const { data } = await axios.get(process.env.OPEN_WEATHER_URL + url, params);
//   return data;
// };
